import { View, Text, SafeAreaView } from 'react-native';
import React from 'react';

const About = () => {
  return (
    <SafeAreaView>
      <Text>About</Text>
    </SafeAreaView>
  );
};

export default About;
